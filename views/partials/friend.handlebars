<li>
	{{name}}

	<button class="change" data-id="{{id}}" data-change="{{male}}">
		{{#if male}}To Male{{else}}To Female{{/if}}
	</button>

	<button class="delete" data-id="{{this.id}}">DELETE</button>
</li>
